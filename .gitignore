node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store

# Playwright MCP scratch snapshots
.playwright-mcp/

# Local visual-QA screenshots (loose sign-off captures, not committed)
qa*.png
# Any loose root-level QA capture (batch/feature-prefixed, e.g. b6-*, i3-*, lang-*);
# committed images live under app/ and src/, never repo root.
/*.png
