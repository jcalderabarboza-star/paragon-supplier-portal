// SDC — Supplier Data Collaboration. SDC-0: types + SIMULATED fixtures (the
// spine — no flows, no CommandTargets, no UI; wiring lands SDC-2/SDC-3; see
// types.ts, fixtures.ts, FLAGS.md). SDC-1a: the P2 planner-consolidation
// read-model (pure selectors over the fixtures; consolidation.ts).
export * from './types';
export * from './fixtures';
export * from './consolidation';
