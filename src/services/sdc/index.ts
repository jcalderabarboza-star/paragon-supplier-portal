// SDC-0 — Supplier Data Collaboration data model. Types + SIMULATED fixtures.
// The spine only (no flows, no CommandTargets, no UI, no registry touch); wiring
// lands SDC-2/SDC-3. See types.ts, fixtures.ts, and FLAGS.md.
export * from './types';
export * from './fixtures';
